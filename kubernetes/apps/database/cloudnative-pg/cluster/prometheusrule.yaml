---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/prometheusrule_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: cloudnative-pg-backups
spec:
  groups:
    - name: cloudnative-pg.backups
      rules:
        - alert: CNPGBackupStale
          expr: time() - max by (cluster) (cnpg_backup_stopped_at{phase="completed"}) > 36 * 3600
          for: 30m
          labels:
            severity: critical
          annotations:
            summary: "CNPG {{ $labels.cluster }}: last base backup older than 36h"
        - alert: CNPGBackupMissing
          expr: absent(cnpg_backup_stopped_at{phase="completed"})
          for: 2h
          labels:
            severity: critical
          annotations:
            summary: "No completed CNPG backup is visible to Prometheus"
        - alert: CNPGWALArchiveFailing
          expr: increase(cnpg_collector_pg_wal_archive_status{value="failed"}[30m]) > 0
          for: 15m
          labels:
            severity: critical
          annotations:
            summary: "CNPG {{ $labels.cluster }}: WAL archiving to R2 is failing"
